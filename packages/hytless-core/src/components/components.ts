import { ComponentData } from '../models/component.models';

/**
 * 
 * @param name 
 * @param options 
 * @param data 
 * @param methods 
 * @param handlers 
 * @param components 
 * @returns 
 */
export function createHytless (
  data: ComponentData
): ComponentData {
  return data;
}